import 'package:flutter/material.dart';

class EternityMode extends StatelessWidget {
  const EternityMode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Eternity Mode'),
        centerTitle: true,
      ),
    );
  }
}
